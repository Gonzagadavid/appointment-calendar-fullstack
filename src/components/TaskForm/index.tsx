import React, { useContext } from 'react';

function TaskForm() {
  return (
    <form>
      <input placeholder="Titulo" />
      <input placeholder="Titulo" />
    </form>
  );
}

export default TaskForm;
